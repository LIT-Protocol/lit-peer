This folder contains partially static and partially generated content.

At least the ERC20.json is statically added and will not be regenerated if removed, so

DO NOT TRY TO DELETE AND RECREATE THIS FOLDER

just run `make update` and it will overwrite the abis with any changed contract code.